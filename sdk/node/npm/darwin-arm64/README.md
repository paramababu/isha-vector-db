# @isha-vector-db/node-darwin-arm64

The native addon for `darwin-arm64`, built from `aarch64-apple-darwin`.

Not installed directly. `@isha-vector-db/node` declares every platform package as an
optional dependency with `os` and `cpu` constraints, so npm downloads only the one that
matches the machine doing the installing.
